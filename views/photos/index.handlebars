<div class="col-md-12">
  <div class="row">
    <h2> {{photo.title}}</h2>
  </div>
  <div class="col-md-6">
    <img class="img-responsive" src={{photo.image_url}}>
    <p>{{photo.description}}</p>
    <form>
    	<button class="btn btn-primary">Delete This Photo</button>
    </form>		    
  </div>
</div>